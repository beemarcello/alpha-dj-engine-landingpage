# Alpha DJ Engine
User-agent: *
Allow: /

# Stand 2026-08-31: keine Seite traegt mehr ein noindex, alle stehen in der
# Sitemap — auch die Rechtstexte. Wer eine Seite wieder auf noindex setzt,
# nimmt sie aus STATIC_PAGES in src/build-sitemap.mjs raus; noindex + Sitemap
# ist ein Widerspruch, den die Search Console meldet. Kein Disallow: alles
# soll erreichbar und crawlbar bleiben.

# /knowledgebase/ ist die Wissensbasis des AI-Support-Agents und MUSS crawlbar
# und indexierbar bleiben — der Agent kann nur indexierbare Seiten lesen.
# Hier niemals ein Disallow eintragen, und in der Seite kein noindex setzen.

Sitemap: https://alpha-dj-engine.com/sitemap.xml
