# Alpha DJ Engine
User-agent: *
Allow: /

# Alle KUNDENSEITEN sind indexierbar und stehen in der Sitemap — auch die
# Rechtstexte. Wer eine davon auf noindex setzt, nimmt sie aus STATIC_PAGES in
# src/build-sitemap.mjs raus; noindex + Sitemap ist ein Widerspruch, den die
# Search Console meldet.
#
# Ausnahme: /_brand/preview.html ist eine interne Design-Vorschau. Sie traegt
# noindex und steht nicht in der Sitemap. Absichtlich KEIN Disallow: das wuerde
# nur das Crawlen verhindern, nicht das Indexieren — und ein nicht gecrawltes
# noindex wird nie gelesen.

# /knowledgebase/ ist die Wissensbasis des AI-Support-Agents und MUSS crawlbar
# und indexierbar bleiben — der Agent kann nur indexierbare Seiten lesen.
# Hier niemals ein Disallow eintragen, und in der Seite kein noindex setzen.

Sitemap: https://alpha-dj-engine.com/sitemap.xml
