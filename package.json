{
  "name": "amc-landingpage",
  "version": "1.0.0",
  "private": true,
  "description": "Alpha Music Converter — Landingpage. Build-Schritt nur fuer Tailwind und die Icons.",
  "scripts": {
    "build:css": "tailwindcss -i src/tailwind.css -o css/tailwind.css --minify",
    "watch:css": "tailwindcss -i src/tailwind.css -o css/tailwind.css --watch",
    "build:icons": "node src/build-icons.mjs",
    "build:pages": "node src/build-pages.mjs",
    "build:sitemap": "node src/build-sitemap.mjs",
    "build": "npm run build:pages && npm run build:css && npm run build:icons && npm run build:horse && npm run build:sitemap",
    "build:horse": "node src/build-horse.mjs"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "lucide-static": "^0.400.0"
  }
}
